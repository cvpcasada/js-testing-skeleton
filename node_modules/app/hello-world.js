'use strict';

var template = require('./templates/hello-world.hbs');

function HelloWorld(opts) {
	if (!(this instanceof HelloWorld)) {
		return new HelloWorld(opts);
	}
}

HelloWorld.prototype.getMessage = function() {
	return template();
};

module.exports = HelloWorld;
