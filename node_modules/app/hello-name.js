'use strict';

var template = require('./templates/hello-name.hbs');

function HelloName(opts) {
	if (!(this instanceof HelloName)) {
		return new HelloName(opts);
	}
}

HelloName.prototype.getMessage = function(name) {
	return template({name: name});
};

module.exports = HelloName;
